version: 2.1
orbs:
  node: circleci/node@4.7.0
workflows:
  node-tests:
    jobs:
      - node/test:
          app-dir: "~/new-project"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
      - run: npm ci
      - run: npm test
